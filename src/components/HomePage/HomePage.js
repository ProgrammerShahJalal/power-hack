import React from 'react';
import Layout from '../Layout/Layout';
import Hero from './Hero';

const HomePage = () => {
  return (
    <Layout>
     <Hero/>
    </Layout>
  );
};

export default HomePage;
